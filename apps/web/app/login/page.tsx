import { LockKeyhole } from "lucide-react";
import { LoginForm } from "../../components/login-form";
import { prototypeRuntimeEnabled, reviewRuntimeEnabled } from "../../lib/config";

export default function LoginPage(){const prototype = prototypeRuntimeEnabled(); const review = reviewRuntimeEnabled(); const personas = prototype || review; return <main className="dot-grid grid min-h-dvh place-items-center bg-[#0d0f19] p-5 text-white"><section className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.055] p-7 shadow-2xl backdrop-blur-xl"><span className="grid size-12 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 text-lg font-black">A</span><h1 className="display mt-7 text-3xl font-bold">Welcome to AndThenn.</h1><p className="mt-2 text-sm leading-6 text-zinc-400">{prototype ? "Choose a local persona. No external account or connection is required." : review ? "Choose a review persona. This shared environment contains demonstration data only." : "A quieter place to run the busy parts of creative work."}</p><LoginForm prototype={personas}/><p className="mt-6 flex items-center justify-center gap-2 text-[10px] text-zinc-600"><LockKeyhole size={12}/> {prototype ? "Local prototype · simulated services" : review ? "Protected review environment · simulated services" : "Invite-only workspace · protected activity history"}</p></section></main>}

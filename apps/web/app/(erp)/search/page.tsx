import { Badge } from "@andthenn/ui"; import { Search } from "lucide-react"; import { PageHeading } from "../../../components/page-heading";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q="Aster"}=await searchParams;return <><PageHeading title="Search" description="Results are permission-scoped using the same policies as record detail routes."/><div className="surface flex h-14 items-center gap-3 rounded-2xl px-4"><Search size={18} className="text-violet-500"/><input defaultValue={q} className="flex-1 bg-transparent font-semibold outline-none" aria-label="Search query"/></div><div className="mt-4 max-w-3xl space-y-2">{[{title:"Aster House — Monsoon Stories",meta:"Project · AT-042",tone:"violet"},{title:"Hero film — edit V2",meta:"Task · Aster House",tone:"cyan"},{title:"Aster House",meta:"Client · 3 active projects",tone:"green"}].map(result=><button key={result.title} className="surface flex w-full items-center gap-4 rounded-2xl p-4 text-left hover:border-violet-300"><span className="grid size-10 place-items-center rounded-xl bg-zinc-950 text-xs font-bold text-white">{result.title[0]}</span><span className="flex-1"><span className="block text-sm font-bold">{result.title}</span><span className="text-xs text-zinc-400">{result.meta}</span></span><Badge tone={result.tone as "violet"|"cyan"|"green"}>Match</Badge></button>)}</div></>}

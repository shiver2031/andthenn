import { Badge, Button } from "@andthenn/ui";
import { Grid2X2, List, Plus, Search } from "lucide-react";
import Link from "next/link";
import { PageHeading } from "../../../components/page-heading";
import { projects } from "../../../lib/demo-data";
export default function ProjectsPage() { return <><PageHeading eyebrow="12 active" title="Projects" description="Every engagement, deadline and delivery thread in one place." action={<Button><Plus size={16}/> New project</Button>} /><div className="surface mb-4 flex items-center gap-2 rounded-2xl p-2"><div className="flex h-10 flex-1 items-center gap-2 px-2"><Search size={16} className="text-zinc-400"/><input aria-label="Search projects" placeholder="Search projects…" className="w-full bg-transparent text-sm outline-none"/></div><button className="grid size-9 place-items-center rounded-lg bg-zinc-100"><Grid2X2 size={15}/></button><button className="grid size-9 place-items-center rounded-lg text-zinc-400"><List size={15}/></button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.concat([{id:"northstar",code:"AT-045",name:"Northstar — Brand Launch",client:"Northstar Hotels",owner:"Maya",due:"23 Aug",progress:18,health:"Planning",tone:"violet"}]).map((project)=><Link href={`/projects/${project.id}`} key={project.id} className="surface group rounded-2xl p-5 transition-transform hover:-translate-y-0.5"><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-xl bg-zinc-950 text-xs font-black text-white">{project.client.split(" ").map(x=>x[0]).join("")}</span><Badge tone={project.tone === "amber" ? "amber" : project.tone === "green" ? "green" : "violet"}>{project.health}</Badge></div><p className="mt-5 text-[10px] font-bold uppercase tracking-widest text-zinc-400">{project.code} · {project.client}</p><h2 className="mt-1 text-base font-bold group-hover:text-violet-600">{project.name}</h2><div className="mt-5 flex items-center justify-between text-xs"><span className="text-zinc-400">Owner {project.owner}</span><span className="font-bold">Due {project.due}</span></div><div className="mt-3 h-1.5 rounded-full bg-zinc-100"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 to-cyan-400" style={{width:`${project.progress}%`}}/></div></Link>)}</div></> }

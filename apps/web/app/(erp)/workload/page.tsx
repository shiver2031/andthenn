import { Button } from "@andthenn/ui";
import { Download, SlidersHorizontal } from "lucide-react";
import { PageHeading } from "../../../components/page-heading";
import { WorkloadGrid } from "../../../components/workload-grid";
export default function WorkloadPage(){return <><PageHeading eyebrow="Studio capacity" title="Workload" description="Primary-owner commitments against individual working capacity. Collaborator load stays visible but separate." action={<div className="flex gap-2"><Button variant="secondary"><SlidersHorizontal size={15}/> Filters</Button><Button variant="secondary"><Download size={15}/> Export</Button></div>}/><div className="mb-4 grid gap-3 sm:grid-cols-3"><div className="surface rounded-2xl p-4"><p className="text-xs font-semibold text-zinc-400">Studio utilisation</p><p className="display mt-2 text-2xl font-bold">74%</p></div><div className="surface rounded-2xl p-4"><p className="text-xs font-semibold text-zinc-400">At capacity risk</p><p className="display mt-2 text-2xl font-bold text-amber-600">1 person</p></div><div className="surface rounded-2xl p-4"><p className="text-xs font-semibold text-zinc-400">Missing estimates</p><p className="display mt-2 text-2xl font-bold">6 tasks</p></div></div><WorkloadGrid/><p className="mt-3 text-xs leading-5 text-zinc-400">Capacity is an operational planning aid, never a performance ranking. Open a cell to inspect its source tasks and assignments.</p></>}

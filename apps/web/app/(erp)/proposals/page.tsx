import { Badge, Button } from "@andthenn/ui";
import { ArrowRight, FileText, Plus } from "lucide-react";
import { PageHeading } from "../../../components/page-heading";
const proposals=[{id:"PR-028",name:"Northstar Brand Launch",client:"Northstar Hotels",value:"₹8,40,000",age:"Today",deliverables:8},{id:"PR-027",name:"Festive Social Retainer",client:"Morrow & Co.",value:"₹4,75,000",age:"Yesterday",deliverables:12}];
export default function ProposalsPage(){return <><PageHeading eyebrow="2 pending decisions" title="Proposals" description="Shape incoming opportunities, retain their source lineage, and move approved work through activation." action={<Button><Plus size={16}/> New proposal</Button>}/><div className="grid gap-4 lg:grid-cols-2">{proposals.map(item=><article key={item.id} className="surface rounded-2xl p-5"><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-violet-50 text-violet-600"><FileText size={18}/></span><Badge tone="amber">Pending</Badge></div><p className="mt-5 text-[10px] font-bold uppercase tracking-widest text-zinc-400">{item.id} · {item.age}</p><h2 className="display mt-1 text-xl font-bold">{item.name}</h2><p className="mt-1 text-sm text-zinc-400">{item.client}</p><div className="my-5 grid grid-cols-2 rounded-xl bg-zinc-50 p-4"><div><p className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">Estimate</p><p className="mt-1 font-bold">{item.value}</p></div><div><p className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">Deliverables</p><p className="mt-1 font-bold">{item.deliverables}</p></div></div><div className="flex gap-2"><Button size="sm">Review proposal <ArrowRight size={14}/></Button><Button variant="secondary" size="sm">Reject</Button></div></article>)}</div><section className="mt-6 rounded-2xl border border-dashed border-zinc-300 p-6 text-center"><p className="text-sm font-bold">Approved proposals enter activation</p><p className="mt-1 text-xs text-zinc-400">Client → ownership → deadline → deliverables → workflow → team → commercial → explicit activation</p></section></>}

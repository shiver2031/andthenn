import { Button } from "@andthenn/ui"; import { Bell, CheckCheck } from "lucide-react"; import { PageHeading } from "../../../components/page-heading"; import { notifications } from "../../../lib/demo-data";
export default function NotificationsPage(){return <><PageHeading eyebrow="2 unread" title="Notifications" description="Assignments, mentions, risks, review activity, account expiry, and integration failures." action={<Button variant="secondary"><CheckCheck size={15}/> Mark all read</Button>}/><div className="surface max-w-3xl overflow-hidden rounded-2xl">{notifications.map(note=><article key={note.title} className={`flex gap-4 border-b border-zinc-100 p-5 ${note.unread?"bg-violet-50/40":""}`}><span className={`mt-1 grid size-9 shrink-0 place-items-center rounded-xl ${note.unread?"bg-violet-600 text-white":"bg-zinc-100 text-zinc-500"}`}><Bell size={15}/></span><div className="flex-1"><h2 className="text-sm font-bold">{note.title}</h2><p className="mt-1 text-xs text-zinc-500">{note.detail}</p></div><time className="text-[10px] text-zinc-400">{note.time}</time></article>)}</div></>}

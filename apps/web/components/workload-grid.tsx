"use client";
import { Badge, Button } from "@andthenn/ui";
import { ChevronLeft, ChevronRight, TriangleAlert } from "lucide-react";
import { people } from "../lib/demo-data";
const days=["Mon 03","Tue 04","Wed 05","Thu 06","Fri 07"];
const allocations=[[6,7,8,5,2.5],[8,8,9,7.2,5],[4,6,5.5,6,4.1],[2,3,2,2.5,2]];
export function WorkloadGrid(){return <div className="surface overflow-hidden rounded-2xl"><div className="flex flex-wrap items-center gap-2 border-b border-zinc-100 p-4"><Button variant="secondary" size="sm"><ChevronLeft size={14}/></Button><Button variant="secondary" size="sm"><ChevronRight size={14}/></Button><p className="ml-2 text-sm font-bold">03–07 August 2026</p><Badge tone="cyan" className="ml-auto">Primary ownership load</Badge></div><div className="overflow-x-auto"><div className="min-w-[850px]"><div className="grid grid-cols-[260px_repeat(5,1fr)] border-b border-zinc-100 bg-zinc-50/70"><div className="p-4 text-[10px] font-bold uppercase tracking-widest text-zinc-400">Team member</div>{days.map(day=><div className="border-l border-zinc-100 p-4 text-center text-[10px] font-bold uppercase tracking-widest text-zinc-400" key={day}>{day}</div>)}</div>{people.map((person,row)=><div key={person.name} className="grid grid-cols-[260px_repeat(5,1fr)] border-b border-zinc-100"><div className="flex items-center gap-3 p-4"><span className="grid size-9 place-items-center rounded-full bg-zinc-950 text-[10px] font-bold text-white">{person.initials}</span><span className="min-w-0 flex-1"><span className="block text-xs font-bold">{person.name}</span><span className="text-[10px] text-zinc-400">{person.scheduled}h / {person.capacity}h</span></span><span className={`text-xs font-black ${person.load>85?"text-amber-600":"text-zinc-500"}`}>{person.load}%</span></div>{allocations[row]!.map((hours,col)=>{const overload=hours>8; const intensity=Math.min(hours/9,1);return <button key={days[col]} title={`${person.name}: ${hours} hours`} className="relative border-l border-zinc-100 p-2 hover:bg-zinc-50"><span className="flex h-12 items-center justify-center rounded-xl text-xs font-bold" style={{backgroundColor:`rgba(${overload?"245,158,11":"109,68,229"},${.07+intensity*.18})`,color:overload?"#b45309":"#5b36c7"}}>{hours}h{overload&&<TriangleAlert size={12} className="ml-1"/>}</span></button>})}</div>)}</div></div></div>}
